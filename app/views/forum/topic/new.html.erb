<div class="forum topic container">
  <h3>Create a new topic</h3>
  <%= form_for(@topic, url: new_topic_path, method: :post) do |f| %>
    <% if @topic.errors.any? %>
      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg%></li>
      <% end %>
      </ul>
    <% end %>

    <p>
      <%= f.label :title %><br />
      <%= f.text_field :title %>
    </p>

    <input style="display: none;" value=<%= current_user.id %> name="topic[creator_id]"/>
    <input style="display: none;" value=<%= @forum.id %> name="topic[forum_id]"/>

    <p><%= f.submit "Create" %></p>
  <% end %>
</div>

<%= debug params %>
